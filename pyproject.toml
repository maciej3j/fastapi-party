[project]
name = "fastapi-party"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.18.3",
    "fastapi[standard]>=0.128.0",
    "pytest>=9.0.2",
    "sqlmodel>=0.0.31",
]
